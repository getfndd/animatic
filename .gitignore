# Binary capture assets (too large for git)
prototypes/**/captures/
prototypes/**/*.mov
prototypes/**/*.mp4
prototypes/**/*.webm
prototypes/**/*.gif
prototypes/**/*.png

# Keep HTML and JSON only in prototypes
!prototypes/**/*.html
!prototypes/**/*.json
!prototypes/**/meta.json

# Environment (secrets)
.env

# macOS
.DS_Store

# Remotion renders (generated video output)
renders/

# Node
node_modules/
package-lock.json
../animatic-corrupted-backup/
out/
package-lock 2.json
src/remotion/preview-props.json

# Claude Code session state (per-user, per-machine)
.claude/knowledge/
.claude/projects/
.claude/project.json

# Codex agent tooling (per-user, per-machine — mirror of the Claude config)
# NOTE: AGENTS.md is the Codex equivalent of the committed CLAUDE.md. If the
# team decides to support Codex as first-class here, `git add` these instead.
.agents/
.codex/
AGENTS.md
