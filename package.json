{
  "name": "animatic",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-to-preset.mjs",
    "sync:dry": "node scripts/sync-to-preset.mjs --dry-run",
    "setup-hook": "bash scripts/setup-hook.sh"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.49.1",
    "dotenv": "^16.4.7"
  }
}
